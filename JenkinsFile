node {
def app
 environment {
    DOCKERHUB_CREDENTIALS = credentials('dockerhub')
  }
  stage('SCM') {
    checkout scm
  }
  
  stage('SonarQube Analysis') {
    def scannerHome = tool 'sonarqube_scanner';
    withSonarQubeEnv() {
      sh "${scannerHome}/bin/sonar-scanner"
    }
    
  }
  
  
    stage('Build') {
      
    app=docker.build("blog-app:sample")
      
    }
    
  stage('Unit Test')
  {
  
  }
  
  stage('Integration Test')
  {
  
  }
  
  stage('Deployment')
  {
  
  }

  }

  
  
  

